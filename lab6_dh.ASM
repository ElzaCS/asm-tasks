DATA SEGMENT
N DW 12H
ANS DW ?
DATA ENDS

CODE SEGMENT
         ASSUME DS:DATA,CS:CODE
START:
MOV AX, DATA
MOV DS, AX

LEA SI, N
LEA DI, ANS

MOV BL, [SI]
AND BL, 0FH
MOV AL, [SI]
AND AL, 240

MOV CL, 04
ROR AL, CL
MOV DL, 0AH
MUL DL
ADD AL, BL
MOV [DI], AL


CODE ENDS
END START